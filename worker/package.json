{
  "name": "financas-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute financas-db --file=../schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
